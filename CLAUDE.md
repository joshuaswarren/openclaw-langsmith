# openclaw-langsmith

## Privacy Policy

1. **NEVER commit API keys, tokens, or secrets** — even in comments or examples
2. **Config examples must use placeholders** — `${LANGSMITH_API_KEY}`, not actual keys
3. **No personal data in code** — use generic placeholders in all examples
